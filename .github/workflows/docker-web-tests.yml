name: Build and push Docker images

on: 
  workflow_run:
    workflows: ["CMake on a single platform"]
    types:
      - completed

jobs:
  docker:
    runs-on:
      - ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    steps:
      - uses: actions/checkout@v4

      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker images
        run: |
            docker build -t ${{ secrets.DOCKER_USERNAME }}/testrepo_app:latest -f Dockerfile.app .
            docker build -t ${{ secrets.DOCKER_USERNAME }}/testrepo_web:latest -f Dockerfile.web .
            docker push ${{ secrets.DOCKER_USERNAME }}/testrepo_app:latest
            docker push ${{ secrets.DOCKER_USERNAME }}/testrepo_web:latest